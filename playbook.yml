---
- name: Modify user settings
  hosts: localhost
  tasks:
    - name: Ensure a user exists and is in a specific group
      ansible.builtin.user:
        name: myuser
        group: mygroup
        state: present
